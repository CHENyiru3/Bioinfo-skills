# Template only. This file is not expected to build until package pins are
# finalized.
FROM mambaorg/micromamba:latest

COPY envs/scverse-python.yml /tmp/scverse-python.yml
RUN micromamba install -y -n base -f /tmp/scverse-python.yml && micromamba clean -a -y

SHELL ["/bin/bash", "-lc"]
CMD ["python", "-c", "import scanpy as sc; print(sc.__version__)"]

